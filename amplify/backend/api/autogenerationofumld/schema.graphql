type UMLDiagram @model(subscriptions: { level: public }) @auth(rules: [{ allow: owner }]) {
  id: ID!
  key: String!
  name: String!
  description: String
  gender: String
  age: String
  from: String
  to: String
}
